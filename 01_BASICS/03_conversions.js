// conversion to number
let aNumber = undefined


let convertedNumber = Number(aNumber); // convers a variable into type Number

console.log(convertedNumber);
console.log(typeof convertedNumber);

// "33"=> number
// "33abc" => type: number, value: NaN(not a number)
// true => type: number, value: 1
// null => type: number, value: 0
// undefined => type: number, value: NaN


// Moreover we do have other types of converting functions 
// Boolean()
// String()